# Copy this file to .env.local and fill in values

# Optional: API key to secure the /iclock/cdata push endpoint
# DEVICE_API_KEY=your-secret-key

# Optional: Vercel KV — add for persistent storage on Vercel
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
